package brickBreaker;

public class Scores {
    int x, y, currentScore, originalScore;
    public Scores(int x, int y, int score) {
        this.x = x;
        this.y = y;
        originalScore = score;
        currentScore = score;
    }
}

